# Documentation-only security-gate smoke test

This temporary pull request verifies that the stable security integration gate succeeds while its
credential-free live Docker matrix is safely skipped for a documentation-only change.
